info:
  name: Topups Stats (Admin)
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/stats/topups"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
