info:
  name: Shops Stats (Admin)
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/api/stats/shops"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
