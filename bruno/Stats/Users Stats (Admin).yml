info:
  name: Users Stats (Admin)
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/api/stats/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
