info:
  name: Jobs Stats (Admin)
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/stats/jobs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
