info:
  name: Events (Shop)
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/events/:shopId"
  params:
    - name: shopId
      value: "123"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
