info:
  name: Create Owner (Shop)
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/api/owners/:shopId"
  params:
    - name: shopId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
