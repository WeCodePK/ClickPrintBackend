info:
  name: List Owners (Shop)
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/owners/:shopId"
  params:
    - name: shopId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
