info:
  name: List History (Shop)
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/history/shops/:shopId"
  params:
    - name: shopId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
