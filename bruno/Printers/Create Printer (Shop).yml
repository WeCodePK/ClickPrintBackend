info:
  name: Create Printer (Shop)
  type: http
  seq: 4

http:
  method: POST
  url: "{{host}}/api/printers/:shopId"
  params:
    - name: shopId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "name": "HP LaserJet"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
