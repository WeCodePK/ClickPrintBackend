info:
  name: Delete Printer (Shop)
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{host}}/api/printers/:shopId/:printerId"
  params:
    - name: shopId
      value: ""
      type: path
    - name: printerId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
