info:
  name: List Printers (Admin)
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/printers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
