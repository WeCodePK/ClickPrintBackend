info:
  name: Get Printer (Shop)
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/printers/:shopId/:printerId"
  params:
    - name: printerId
      value: ""
      type: path
    - name: shopId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
