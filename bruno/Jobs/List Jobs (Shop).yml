info:
  name: List Jobs (Shop)
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/jobs/shops/:shopId"
  params:
    - name: shopId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
