info:
  name: Get Draft (User)
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/drafts/:draftId"
  params:
    - name: draftId
      value: "{{draftId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
