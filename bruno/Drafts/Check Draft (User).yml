info:
  name: Check Draft (User)
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{host}}/api/drafts/:draftId/check"
  params:
    - name: draftId
      value: 6a5df922314cfee5ef348f5d
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
