info:
  name: List Drafts (User)
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/drafts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
