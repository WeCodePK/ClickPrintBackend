info:
  name: Delete Draft (User)
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{host}}/api/drafts/:draftId"
  params:
    - name: draftId
      value: 6a5df86b314cfee5ef348f5c
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
