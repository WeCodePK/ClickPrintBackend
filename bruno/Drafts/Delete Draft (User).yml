info:
  name: Delete Draft (User)
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{host}}/api/drafts/:draftId"
  params:
    - name: draftId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
