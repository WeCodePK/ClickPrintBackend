info:
  name: Submit Draft (User)
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{host}}/api/drafts/:draftId/submit"
  params:
    - name: draftId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
