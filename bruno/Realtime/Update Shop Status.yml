info:
  name: Update Shop Status
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{host}}/api/realtime/status/:shopId"
  params:
    - name: shopId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
