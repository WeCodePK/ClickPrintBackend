info:
  name: Server Side Events
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/realtime/events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
