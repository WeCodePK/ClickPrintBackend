info:
  name: List Contact Submissions (Admin)
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/contact"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
