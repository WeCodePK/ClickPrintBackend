info:
  name: Delete User (Admin)
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{host}}/api/users/:userId"
  params:
    - name: userId
      value: 6a59ff9aa4f30684c49c0bb7
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
