info:
  name: Update User (User)
  type: http
  seq: 4

http:
  method: PUT
  url: "{{host}}/api/users/:userId"
  params:
    - name: userId
      value: 6a59ff9aa4f30684c49c0bb7
      type: path
  body:
    type: json
    data: |-
      {
        "name": "A Ahad"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
