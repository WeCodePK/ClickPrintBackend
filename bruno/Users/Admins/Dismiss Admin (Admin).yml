info:
  name: Dismiss Admin (Admin)
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{host}}/api/users/admins/:userId"
  params:
    - name: userId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
