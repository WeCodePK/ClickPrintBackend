info:
  name: Appoint Admin (Admin)
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/api/users/admins/:userId"
  params:
    - name: userId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
