info:
  name: Update Shop Status
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{host}}/api/shops/:shopId/status"
  params:
    - name: shopId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
