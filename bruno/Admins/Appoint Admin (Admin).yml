info:
  name: Appoint Admin (Admin)
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/api/admins"
  body:
    type: json
    data: |-
      {
        "user": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
