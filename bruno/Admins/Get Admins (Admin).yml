info:
  name: Get Admins (Admin)
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/admins"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
