info:
  name: Approve/Decline Topup (Admin)
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{host}}/api/topups/:topupId"
  params:
    - name: topupId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "status": "approved"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
