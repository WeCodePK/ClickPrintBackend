info:
  name: Get Topup (User)
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/topups/:topupId"
  params:
    - name: topupId
      value: 6a53290f8b2d1b4cb3601ec5
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
