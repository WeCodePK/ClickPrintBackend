info:
  name: Create Topup (User)
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/api/topups"
  body:
    type: json
    data: |-
      {
        "amount": 1000,
        "ppfid": "95e498e0-ade0-4b12-9317-00772d61142e"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
