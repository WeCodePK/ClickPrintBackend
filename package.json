{
  "name": "clickprintbackend",
  "scripts": {
    "dev": "nodemon --exec \"docker compose build --progress quiet && docker compose up -d && docker compose logs -f --tail 0 backend\""
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "expo-server-sdk": "^6.1.0",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.7.4",
    "morgan": "^1.11.0",
    "multer": "^2.2.0"
  }
}
